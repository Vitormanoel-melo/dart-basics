// you can add void before de main function, but it's not necessary
main() {
  // declare variable without specifying the type, so it accept any value type, number, string, etc.
  var firstName = 'Vitor';

  String lastName = 'Brito';

  print(firstName + ' ' + lastName);
}
